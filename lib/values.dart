class Endpoints {
  Endpoints._();

  // base url
  static const String baseUrl = 'https://gen10.m2mbeta.com';

  // receiveTimeout
  static const receiveTimeout = Duration(seconds: 60);

  // connectTimeout
  static const connectionTimeout = Duration(seconds: 60);

  // static const String times = 'api/v1/attendance/get/year/ess?year=';
}
